import { site } from "@/content/site";

/** Built by app/og.png/route.tsx at build time. */
export const ogImage = {
  url: "/og.png",
  width: 1200,
  height: 630,
  alt: `${site.name}: ${site.positioning}`,
};

export const defaultTitle = `${site.shortName} | CS @ Georgia Tech`;
